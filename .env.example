# Google Cloud
GCP_PROJECT_ID=
GCP_LOCATION=global

# Local server
PORT=3003

# Opcional (local): caminho para o JSON da service account
# No PowerShell você pode usar $env:GOOGLE_APPLICATION_CREDENTIALS="C:\\caminho\\sa.json"
# Este projeto usa ADC (gcloud auth application-default login) OU GOOGLE_APPLICATION_CREDENTIALS.
# Client ID/Secret (OAuth) não é necessário para o backend neste MVP.
GOOGLE_APPLICATION_CREDENTIALS=

# Se você estiver usando OAuth no frontend (não implementado aqui), você pode manter:
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
