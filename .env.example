# Google Cloud
GCP_PROJECT_ID=
GCP_LOCATION=global

# Required for PDF -> DOCX (Word) via batch translation
GCS_TRANSLATION_BUCKET=

# Local server
PORT=3003

# Upload limits
MAX_FILES=200
IMAGE_BATCH_SIZE=10

# Opcional (local): caminho para o JSON da service account
GOOGLE_APPLICATION_CREDENTIALS=

# OAuth (não usado no backend deste projeto)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
